import React, { useState } from "react";

const Test = ({ message }) => {
    return (
        <div>
            <a>to jest wiadomosc show</a> <h1>{message}</h1>
        </div>
    );
};

export default Test;
